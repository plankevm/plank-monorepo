import std::core::regions::*;

const return_runtime = fn () never {
    let buf = malloc_uninit(@runtime_length());
    unsafe_codecopy(buf, rawptr(code) { raw: @runtime_start_offset() }, @runtime_length());
    unsafe_return(buf, @runtime_length());
};
