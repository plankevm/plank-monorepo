const max = fn (a: u256, b: u256) u256 {
    if gt(a, b) { a } else { b }
};

const min = fn (a: u256, b: u256) u256 {
    if lt(a, b) { a } else { b }
};

const ceil32 = fn (x: u256) u256 {
    let remainder = mod(x, 32);
    if eq(remainder, 0) { x } else { add(x, sub(32, remainder)) }
};
